---
author: "Daniel Vila (Quobis)"
description: "Several Calls Headless Test"
descriptor_schema: https://raw.githubusercontent.com/sonata-nfv/tng-schema/master/test-descriptor/testdescriptor-schema.yml
name: "comm-pilot-test"
vendor: "quobis"
version: '0.1'

service_platforms:
  - "OSM"
  - "SONATA"
test_category:
  - "basic-tests"

phases:
  - id: setup
    steps:
      - action: deploy
        description: "Deploying a NS"
        name: deployment
      - action: configure
        description: "Configuration"
        name: configuration
        probes:
          - id: several-calls
            description: "Headless browser called with Puppeteer"
            image: "danielvifal/several-calls:latest"
            name: several-calls
            parameters:
              - key: WEB
                value: 'https://comm-pilot.5gtango.eu'
              - key: USER
                value: 'apps'
              - key: RP_IP
                value: '$(rp-vnf/endpoints/id:mgmt/address)'
              - key: MS_IP
                value: '$(ms-vnf/endpoints/id:mgmt/address)'

  - id: exercise
    steps:
      - command: /bin/sh
        dependencies: []
        description: "Starting the initiator"
        entrypoint: /entrypoint.sh
        index: 1
        instances: 1
        name: several-calls
        output:
          - {results: results.log}
        run: several-calls

  - id: verification
    steps:
      - step: several-calls
        description: "Check obtained results"
        name: several-calls
        conditions:
          - condition: present
            file: results.log
            find: TEST PASSED
            name: headless-ok
            verdict: pass
